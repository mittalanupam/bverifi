# Railway config for frontend (used when deploying with Nixpacks, not Docker)
[build]
builder = "nixpacks"

[deploy]
# Must bind to 0.0.0.0 so Railway's proxy can reach the app (not localhost)
startCommand = "serve -s build -l 0.0.0.0:$PORT"
